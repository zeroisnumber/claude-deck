[package]
name = "claude-deck"
version = "0.5.20"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon", "image-png"] }
tauri-plugin-updater = "2"
tauri-plugin-process = "2"
tauri-plugin-dialog = "2"
tauri-plugin-notification = "2"
tauri-plugin-window-state = "2"
tauri-plugin-clipboard-manager = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
portable-pty = "0.8"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls", "json"] }
base64 = "0.22"
dirs = "5"
# WebView2 초기 IME 바인딩 버그 우회용 (tauri가 쓰는 버전과 일치해야 함)
webview2-com = "0.38"
# 패닉 발생 시 네이티브 오류 팝업 표시용. tauri-plugin-dialog가 이미 물고 오는
# 버전을 그대로 재사용 — msgbox는 gtk-sys 버전이 충돌해서 못 씀. Windows 네이티브
# 백엔드만 쓰므로 기본 피처(xdg-portal 등)는 불필요.
rfd = { version = "0.15", default-features = false }
# 세션을 가져간 프로세스를 끌 때만 쓴다. 번호만 믿지 않고 시작 시각까지 맞춰 보려면
# GetProcessTimes가 필요하다. tauri가 이미 같은 버전을 물고 있어 빌드가 늘지 않는다.
windows-sys = { version = "0.59", features = ["Win32_Foundation", "Win32_System_Threading"] }

[profile.dev]
opt-level = 1
